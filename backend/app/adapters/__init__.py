"""Concrete port implementations.

This is the only package that may import an infrastructure SDK. Each subpackage is
selected by name from Settings in app/composition, never imported directly by an
application module.
"""
