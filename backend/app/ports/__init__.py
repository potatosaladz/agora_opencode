"""Abstract contracts. `docs/PORTS.md` is the normative source for this package.

Nothing here may import an infrastructure SDK, and nothing here may import
`app.adapters` — enforced by `tests/test_layering.py`.
"""
